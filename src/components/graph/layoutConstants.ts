/** Shared sizing used by both the force-directed layout (collision radius)
 * and the floating-edge geometry (where an edge meets a node's boundary). */
export const NODE_WIDTH = 96;
export const NODE_HEIGHT = 96;
/** Roughly the gauge circle's own radius - edges terminate at the ring
 * rather than at the node's full label bounding box. */
export const NODE_RADIUS = 40;
